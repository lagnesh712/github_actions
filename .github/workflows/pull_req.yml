name: pull_req

on:
  pull_request:
    types:
      - "opened"
    branches:
      - "merge_branch"

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      # - name: Checkout
        # uses: actions/checkout@v3
      - name: echo command
        run: echo "hello world this is demo branch"